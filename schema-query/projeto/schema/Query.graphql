# API Entry Points

type Query {
  ola: String!
  horaAtual: Date!
  usuarioLogado: Usuario
  produtoDestaque: Produto
  numerosMegaSena: [Int!]!
  usuarios: [Usuario],
  usuario (id: Int): Usuario,
  perfis: [Perfil],
  perfil(id: Int): Perfil
}